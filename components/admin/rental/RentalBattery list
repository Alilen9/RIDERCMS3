import RentalBatteryCard from './RentalBatteryCard';

const RentalBatteryList = ({
  batteries,
}: any) => {
  return (
    <div>

      <h2 className="text-xl text-white mb-4">
        Rental Batteries
      </h2>

      <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">
        {batteries.map((battery: any) => (
          <RentalBatteryCard
            key={battery.id}
            battery={battery}
          />
        ))}
      </div>

    </div>
  );
};

export default RentalBatteryList;